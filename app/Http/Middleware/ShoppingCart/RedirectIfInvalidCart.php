<?php

namespace App\Http\Middleware\ShoppingCart;

use Closure;

class RedirectIfInvalidCart
{
    /**
     * Handle an incoming request.
     *
     * @param  \Illuminate\Http\Request  $request
     * @param  \Closure  $next
     * @return mixed
     */
    public function handle($request, Closure $next)
    {
        return $next($request);
    }
}
